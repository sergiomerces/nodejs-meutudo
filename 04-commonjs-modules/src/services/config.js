const devArea = {
    version: "01ab",
    production: true,
};

const client = {
    device: "web",
}

module.exports = {
    devArea,
    client
};

//quando tenho apenas um elemento para exportar não preciso usar chaves